- Scale and compress portraits
- Add results not found
    - Also add warning or just a message if a subquery has no results
- Make the portraits themselves grayscale
    - Match the intensity levels
- For the indexes, instead of generating JS objects from Python, use JSON.parse(), it's a lot faster
- Log progress of build to console
- Clear button for search field









- Clicking on logo redirects to awate.in
    - Though this should be fixed with a new domain
- Show number of results: "189 peptides found"